<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="PrimaryButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:background">@color/primary</item>
        <item name="android:textColor">@color/gray_100</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="SecondaryButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:background">@color/gray_600</item>
        <item name="android:textColor">@color/gray_100</item>
        <item name="android:textSize">24sp</item>
    </style>

</resources>
